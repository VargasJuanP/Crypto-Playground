enable

    copy running-config startup-config

    conf term

        hostname nombre

        interface nombre_de_la_interfaz

            speed velocidad (en cisco pt esta 10 , 100 o auto)

            duplex half  (o) duplex full (o) duplex auto

            tx-ring-limit numero (1-32767 en cisco)

            switchport mode trunk

                switchport trunk allowed vlan remove vlan_id

            switchport mode access

                switchport access vlan vlan_id

        vlan vlan_id

            name nombre



ME FALTAN LOS COMANDOS PARA VLANS EN EL RUTER DEL DOT NO SE QUE COSA